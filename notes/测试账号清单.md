# 测试账号清单

> ⚠️ **本文档描述的是"本地账号 + 固定测试账号"时期（2026-09-03）的实现，已过时。**
> 账号后来迁移到服务端 Neon Postgres：`test01~test05` 已取消，admin 密码来自环境变量
> `ADMIN_INITIAL_PASSWORD`，不再是下面的 `admin123`。下面内容仅作历史记录。
>
> **站点公开后的注册规则（当前生效）：**
> - 昵称**必填**，且**不能与他人重复**（大小写不敏感）；改昵称走同一套校验
> - 新注册账号**默认没有测试权限**（`is_test = false`）：要在监控页「测试管理」里由 admin 打开开关
> - 存量账号的 `is_test` 保持不变，要收回权限由 admin 逐个关闭
> - 认领老账号（`/api/auth/claim`）不带来任何权限，伪造 `isAdmin/isTest` 会被忽略

## 管理员账号

| 用户名 | 密码 | 昵称 | 权限 | 等级 | 备注 |
|--------|------|------|------|------|------|
| admin | admin123 | 管理员 | 管理员 + 测试权限 | 15级 | 默认密码，建议修改 |

**重要提示**：如果您之前创建过admin账号使用了旧密码（如`123`或`admin2026`），请清除浏览器localStorage后刷新，系统会自动创建新的admin账号，密码为`admin123`。

## 测试账号

| 用户名 | 密码 | 昵称 | 权限 | 等级 | 备注 |
|--------|------|------|------|------|------|
| test01 | 1234 | 测试用户01 | 测试权限 | 1级 | 四位纯数字密码 |
| test02 | 2345 | 测试用户02 | 测试权限 | 1级 | 四位纯数字密码 |
| test03 | 3456 | 测试用户03 | 测试权限 | 1级 | 四位纯数字密码 |
| test04 | 4567 | 测试用户04 | 测试权限 | 1级 | 四位纯数字密码 |
| test05 | 5678 | 测试用户05 | 测试权限 | 1级 | 四位纯数字密码 |

## 权限说明

### 测试权限（isTest: true）
- 可使用内置测试 API（无需配置自己的 API Key）
- 适用于快速测试游戏功能
- admin 和 test01~test05 均具有此权限

### 管理员权限（isAdmin: true）
- 仅 admin 账号具有
- 包含测试权限的所有功能
- 可访问"测试监控"页面查看所有测试账号使用情况

## 测试监控功能

admin账号登录后，首页会显示"测试监控"卡片（红色主题），点击可查看：

- **统计总览**：总用户数、今日活跃用户、总使用时长
- **用户详情**：每个测试账号的登录次数、使用时长、最后登录时间
- **登录日志**：最近50条登录/登出记录，支持时间筛选
- **Token用量**：占位显示（待后续实现）

## 账号创建机制

### 自动创建
- **时机**：应用启动时（App.vue onLaunch）
- **函数**：`userManager.ensureAdminSeed()`
- **逻辑**：
  1. 检查 admin 账号是否存在，不存在则创建（密码 admin123），存在则更新等级为15级
  2. 检查 test01~test05 是否存在，不存在则批量创建
  3. 已存在的账号不会重复创建，密码保持不变

### 用户注册（已过时，见文首当前规则）
- **入口**：登录页面 → 注册tab
- **昵称**：必填，且不能与他人重复（大写/小写视为同名）
- **权限**：~~新注册账号默认具有测试权限（isTest: true）~~ → 现在默认**没有**测试权限，需 admin 在监控页打开
- **等级**：新注册账号默认1级

## 清除旧账号数据

如果之前创建的admin账号密码不正确，可通过以下方式清除：

### H5端（浏览器）
1. 打开浏览器开发者工具（F12）
2. 进入 Application / 存储 标签
3. 找到 Local Storage → 清除 `sillytroops_users` 和 `sillytroops_current_user_id`
4. 刷新页面，系统会自动创建新账号

### 小程序端
- 在微信开发者工具中：工具 → 清除缓存 → 清除所有数据

## UI显示

### 登录页面
- **测试账号提示**：`test01~test05（密码详见开发文档）`
- **不显示**：admin 账号及密码（避免明文暴露）

### 首页（admin登录）
- 显示"测试监控"功能卡片（第6个位置）
- 显示为 "15级 · 管理员"

## 实施日期
2026-09-03

## 文件修改
- `src/utils/account/userManager.js` - admin密码改为 admin123
- `src/pages/monitor/monitor.vue` - 监控页面完整实现
- `src/pages/index/index.vue` - admin动态菜单

