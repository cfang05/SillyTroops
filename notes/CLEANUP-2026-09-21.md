# 代码注释清理记录

清理日期：2026-09-21
清理范围：`src` 目录下所有 `.vue` / `.js` / `.ts` 文件（共 101 个文件、25,305 行；已跳过 `node_modules`、`dist`、`.uniapp`）

---

## 一、清理统计

| 项目 | 数量 |
|---|---|
| 处理文件数 | **101 个**（`src` 下全部，无遗漏） |
| 删除过时/冗余注释 | **61 处** |
| 修正错误注释 | **32 处** |
| 保留待人工确认（C 类） | **14 处**（另 19 处在复核中被判定为伪问题并驳回） |

合计改动 **93 处注释**。

**代码零改动证明**：对全部 101 个文件，先剥离全部注释（`//`、`/* */`、`<!-- -->`，含跨行块注释状态机），再与 `HEAD` 版本逐文件比对代码 token：

```
identical code (comment-only changes): 96 / 101
```

其余 5 个文件存在**本次清理之前就已存在的未提交代码改动**（与本次任务无关，未被触碰）：

- `src/pages/login/login.vue` — 昵称必填 + 长度校验
- `src/pages/monitor/monitor.vue` — 测试权限开关文案
- `src/pages/settings/settings.vue` — 测试权限相关
- `src/utils/account/userManager.js` — 注册规则 / 老账号认领去掉 isAdmin/isTest
- `src/utils/llm/client.js` — 测试权限错误文案

逐 hunk 核对确认：这 5 个文件的差异**全部**来自上述既有改动，本次注释清理**未在其中丢失任何代码**。

**构建验证**：`npm run build:h5` → `DONE Build complete.`（无报错）

---

## 二、处理明细（按文件）

只列出有改动的文件；未列出者为「已通读、判断无需改动」（67 个文件）。

### src/utils/dice-helper.js
- 删除：14 处（装饰性分隔线 `// ───────`、把代码翻成中文的标签如 `// 参数校验` / `// 清理输入` / `// 加上修正值`、重复的 `// 没有匹配到任何模式`）
- 修正：0 处
- 保留：2 处（优劣式与修正值「语义冲突」的措辞、`"1d6"` vs `"d6"` 示例用词）
- 说明：JSDoc（返回结构、正则模式、检定描述、临界判定）**全部保留**

### src/utils/account/account-manager.js
- 删除：1 处（第 19 行 `// 确保存储初始化（读取一次即可触发初始化）`，该初始化机制在代码中不存在 —— 用户确认删除）
- 修正：0 处

### src/utils/collect/collect-legend_items.js
- 删除：0 处（子 agent 曾误删首行路径注释，已由我**撤销恢复**）
- 修正：0 处
- 保留：3 处（`usable` 行尾注释、`equipable` 行尾注释、`fromStory` 字段无文档且无消费方）

### src/utils/collect/collect-trait.js
- 删除：10 处（`combatBonus` 整段字段说明 + `// 低血量额外加成由 combatManager 动态处理` —— 用户裁决「整句都删掉」）
- 修正：1 处（`见 item-types.js EFFECT_TYPE` → `见 utils/effects/effect-types.js 的 EFFECT_TYPE`，真实定义已迁到该文件）
- 保留：0 处

### src/utils/effects/effect-types.js
- 删除：0 处
- 修正：1 处（使用示例 `require(...)` → ESM `import ...`，全 `src` 无任何 `require()` 调用）
- 说明：文件顶部的效果类型规范说明块完整保留

### src/utils/items/item-manager.js
- 删除：2 处（`旧版含 giveItem/useItem/...`、`直接读写 stateManager（GAME_STATE_*）的方法已移除` —— stateManager 在本仓库全部 git 历史中都不存在）
- 修正：2 处（`无 stateManager 依赖` → `纯注册表`；`当前已加载的角色卡名（避免重复加载）` → 去掉与代码不符的「避免重复加载」，该方法在文件内无读取点）
- 保留：0 处

### src/utils/lore/charCardParser.js
- 删除：4 处（`// content 字段` `// enabled` `// 生成 id` `// regex` 四个纯翻译标签）
- 修正：2 处（`lorebookManager 标准格式` → 指向真实类型 `types/character.ts` 的 `LorebookEntry`；去掉 `（可直接当 characterCard 传给 gameAI）` —— gameAI 不存在）
- 保留：0 处

### src/engine/BlockParser.ts
- 删除：0 处
- 修正：2 处（删掉「迁移自 src/pages/game/game.vue」（该文件从未存在）；`与 utils/security.ts 的 DOMPurify 白名单一致` → 改为准确描述「font/mark 不在白名单内会被剥成纯文字」）
- 说明：正则标签白名单、为何需要 HTML 片段、嵌套退化的解释**保留**

### src/engine/MessageProcessor.ts
- 删除：0 处
- 修正：1 处（原注释称「client.js 未提供 token 级流式接口」与事实相反 —— `generateWithMessagesStream` 与 `_simulateStream` 均为实存代码，改为描述真实分支条件）

### src/engine/WorldInfoEngine.ts
- 删除：1 处（`不修改 utils/lore/lorebookManager.js，TRPG 路径完全不受影响。` → `只服务 chat 模式，不涉及 TRPG 路径。`）
- 修正：1 处（`未被本轮处理的既有 sticky/cooldown：按轮数正常递减` → 与代码一致：「sticky 原值保留、cooldown 按轮数 -1」）
- 保留：分阶段实现说明、酒馆对齐说明

### src/engine/worldInfoMatcher.ts
- 删除：0 处
- 修正：1 处（去掉失效文件名 `lorebookManager.js`，保留「仅服务 chat 模式」的作用域说明）

### src/engine/systemRegex.ts
- 删除：2 处（`scripts/tmp-p4-check.js` 出处子句及其悬空破折号行）
- 修正：0 处
- 说明：**正则坑警示完整保留**（「必须把闭合引号一起传进来组成字符类，否则被当字面量永远匹配不到」）

### src/engine/tokenizer.ts
- 删除：0 处
- 修正：1 处（JSDoc 称 `setTokenCounter`「返回 false 表示未接受」，与 `(fn) => void` 签名矛盾，改为准确描述）
- 保留：改造前自动 `await import` 的性能说明

### src/engine/RegexScriptEngine.ts
- 删除：0 处
- 修正：1 处（`校验脚本的 findRegex（用于预设编辑页做输入校验）` → `校验给定的正则字符串是否合法`；全仓库无调用点，原括注与现状不符）

### src/engine/VariableEngine.ts
- 删除：0 处
- 修正：1 处（`迁移前是不带前缀的 global_variables` → 改为只讲现状「不带前缀的 global_variables 已按既定决策作废重置」，保留「有意不作废兼容读取」的决策信息）

### src/engine/ReasoningHandler.ts
- 删除：0 处
- 修正：1 处（`关闭时行为与改造前完全一致` → `关闭时不做任何思考切分，正文原样返回`，与 `splitReasoning` 关闭分支一致）

### src/engine/MarkdownParser.ts
- 删除：3 处（`// 标题` `// 分隔线` `// 引用`）
- 修正：0 处
- 保留：`// 无序列表` / `// 有序列表`（两分支返回同一 `style: 'list-item'`，标签承载了代码不具备的区分信息）、排序理由注释

### src/engine/PromptBuilder.ts
- 删除：0 处
- 修正：2 处（去掉变更日志式前缀「修复：」，保留「为什么传 vars / 为什么按条目传 depth」的技术说明）
- 保留：token 预算、裁剪、回复预留等全部「为什么」注释

### src/engine/DebugLogger.ts / src/engine/worldInfoMatcher.ts（除上条外） / src/engine/WorldInfoEngine.ts
- 无删除或修正（JSDoc 与性能警示均准确）

### src/types/character.ts
- 删除：0 处
- 修正：7 处
  - `an_top/an_bottom（预留，P2+ 后续功能）` → 去掉「预留」：`WorldInfoEngine` 已分发该桶、`PromptBuilder` 已消费
  - `em_top/em_bottom（预留…）` → 同上
  - `原 story.initialState/scenes/... 的迁移归宿` → 改为只讲现状（story 模块在本仓库全部历史中不存在）
  - `原 story.initialState.scene` → `初始场景`
  - `原 story.initialState.flags` → `初始标记`
  - `原 story.initialQuickActions` → 去掉出处，保留「供聊天页建议行动 UI」
  - 去掉不存在的 `characterStore` 引用
- 保留：lorebook position 枚举说明、分组打分开关说明

### src/types/script.ts
- 删除：0 处
- 修正：1 处（`RegexScriptEngine 只在两处调用：清洗用户输入 / 清洗 AI 回复` → `applyRegexScripts() 共服务四个场景`，与紧邻的 0/1/2/3 编号清单自洽）
- 保留：酒馆原始编号对照表、`ST_TO_INTERNAL_PLACEMENT` 转换警示（完整保留）

### src/types/message.ts
- 删除：0 处
- 修正：1 处（去掉 `兼容现有 game.vue 消息结构`，game.vue 不存在）

### src/stores/runtimeStore.ts
- 删除：0 处
- 修正：1 处（`不再桥接旧 stateManager（已删除）` → `本 store 只保留对话流本身的响应式状态`）

### src/stores/moduleStore.ts
- 删除：0 处
- 修正：2 处（去掉「合并后」「不再是」历史对照，保留「数据源 = 角色卡 extensions.trpg.modules、深合并」等技术事实）

### src/stores/presetStore.ts
- 删除：4 处（`（新建，无现有对应实现）`、`// Sprint 1.7`、重复的内置预设说明两行）
- 修正：0 处
- 保留：1 处（内置预设移除说明的复核留痕，信息在 `builtinAssets.ts` 有更完整记载）

### src/stores/characterCardStore.ts
- 删除：0 处
- 修正：1 处（去掉不存在的 `characterStore（玩家 Role）`，改为指向真实模块 `utils/persona/trpgProfile.js`）

### src/stores/pluginStore.ts
- 删除：0 处
- 修正：0 处
- 保留：1 处 C 类（`// Sprint 1.5` 迭代标记）

### src/utils/account/userScope.js
- 删除：0 处
- 修正：1 处（`用于跨用户读取，如管理场景` → `用于跨账号迁移：读写他人 uid 名下的遗留存档`，与唯一调用方 `migrateForeignLegacy` 一致）

### src/utils/account/personaManager.js
- 删除：0 处
- 修正：1 处（`与 TRPG 的 character-manager.js 不同` → 指向真实模块 `utils/persona/trpgProfile.js`）
- 保留：1 处 C 类（「只承载对话身份」与 `trpgProfile` 字段清单的出入）

### src/utils/account/conversationManager.js
- 删除：0 处
- 修正：1 处（`重置内存缓存并重新初始化（…；账号切换也走它）` → 去掉与事实不符的「账号切换也走它」，全仓库仅 `backupService` 一处调用）
- 保留：v1→v2 迁移、D20 跨账号迁移、初始化顺序 bug 等**全部活代码说明**

### src/utils/character_card/characterCardManager.js
- 删除：0 处
- 修正：2 处（去掉 `utils/character_info/character-manager.js` 两处失效路径引用，保留「与 TRPG 数值档案完全独立」的概念对比）
- 保留：1 处 C 类（`getNPCDef`）

### src/utils/persona/trpgProfile.js
- 删除：0 处
- 修正：1 处（删掉「从 utils/character_info/character-manager.js 迁移而来」及「迁移自 character-manager.js 的函数」出处块，保留设计原则与函数清单）

### src/utils/llm/intentParser.js
- 删除：0 处
- 修正：2 处（`供 gameAI.js / narrator.js / stateManager 等模块共享` → 指向真实消费方 `pages/chat/chat.vue`；`意图解析已合并到 gameAI.js 的单次 LLM 调用` → 改为描述真实的 `detectIntent` 关键词匹配）

### src/utils/llm/client.js
- 删除：12 处（`// 调用底层 API` `// 解析 JSON` `// 验证字段完整性` `// 禁止词检测` `// 所有重试都失败` `// 检查云开发 AI 功能是否可用` `// 创建模型实例` `// 尝试直接解析` 等纯翻译标签）
- 修正：1 处（去掉 `（Sprint：预设编辑页补齐字段后新增）` 历史出处）
- 保留：1 处 C 类（`等实现真正的 SSE 解析后再打开这个开关`）
- 保留：**`ai-helper.js` 的三处引用全部保留** —— 它解释的是现存活代码（`uni.cloud.extend.AI` 分支）为什么长这样，属「为什么这么写」
- 保留：重试/兜底/SSE 解析/`resp.json()` 踩坑等全部技术说明

### src/utils/storage/adapter.ts
- 删除：0 处
- 修正：1 处（`（P5 才会真正接管业务数据）` → 改为现状：IndexedDB 已承载对话/角色卡/预设/Persona 落盘）
- 保留：`每次保存都失败` 的踩坑说明

### src/utils/storage/cachedStore.ts
- 删除：0 处
- 修正：1 处（去掉 `（= 改造前的行为）`）
- 保留：hydrate 竞态、迁移顺序、数据脆弱性等**全部关键说明**

### src/utils/items/common-items.js / src/utils/items/item-types.js
- 删除：0 处
- 修正：0 处
- 说明：子 agent 曾上报 15 条 C 类，经复核**全部为伪问题**（分区标题与 JSDoc 图例重复、字段说明准确等），已驳回。全部保留。

### src/utils/security.ts
- 删除：0 处 / 修正：0 处
- 保留：1 处 C 类（安全白名单描述不完整）

### src/pages/chat/chat.vue（228 行注释，本批注释密度最高）
- 删除：4 处
  - 已修复 bug 的历史叙述（「原来这里调用的是不存在的 parseBlock() / runtimeStore.forceUpdate()」—— 两个符号在全仓库均不存在，bug 早已修好）
  - `// 世界书限时效果（sticky/cooldown）状态，随会话持久化，P2 阶段新增` → 去掉阶段标记
  - `（与原先独立"续写"按钮的条件一致）` 被删除后已由我恢复
- 修正：4 处
  - `改造前：每个 chunk 都替换整个 messages 数组` → `反例（不要退回这种写法）：…`（保留 O(n²) 警示）
  - `改造前的问题：输出侧正则只在生成结束后跑一次` → `必须逐帧处理的原因：若…`
  - `两点与改造前不同` → `两点行为约束（都是踩过坑后加的，不要去掉）`
  - 恢复被过度删除的 `pages.json navigationBarTitleText 只是静态默认值` 与「续写按钮条件」说明
- 保留：**流式帧率节流、平滑输出、显示态管线、三路正则合并、防止登录态失效写入错误文案、停止生成收尾、命令式滚动被刻意移除的原因**等全部 C1 级「为什么」注释；只移除明确过时的 1 行（parseBlock/forceUpdate）

### src/pages/cardpool/cardpool.vue
- 删除：0 处
- 修正：1 处（去掉已不存在的 `.mkt-head` / `.mkt-exit` 局部副本出处，保留「避免样式各自漂移」）
- 保留：1 处 C 类（**同时发现真实代码缺陷**，见下）

### src/pages/cardpool/components/CardDetail.vue
- 删除：0 处
- 修正：0 处
- 保留：96 条注释全部复核，未发现过时或冗余（PC 端布局、评分、导入、删除等说明均准确）

### src/pages/cardpool/components/FilterDrawer.vue / src/pages/cardpool/utils/*.js
- 删除：0 处 / 修正：0 处（`comments.json` / `manifest.json` 等 R2 上传物引用经核实真实有效）

### src/pages/settings/settings.vue
- 删除：3 处（`<!-- 使用说明 -->` `<!-- 模型选择 -->` `<!-- API配置 -->`，与相邻可见标题逐字重复）
- 修正：0 处

### src/pages/monitor/monitor.vue
- 删除：2 处（`// 检查管理员权限`、`// 计算统计数据`）
- 修正：0 处

### src/pages/index/index.vue
- 删除：0 处
- 修正：1 处（`已迁移到 components/common/TabBar.vue` → `样式统一收在 components/common/TabBar.vue`，保留「避免两处样式各自漂移」）

### src/pages/presets/edit.vue
- 删除：1 处（`<!-- marker 提示词说明 -->`）
- 修正：0 处

### src/pages/import/import.vue
- 删除：0 处
- 修正：1 处（守卫注释 `未登录会被 reLaunch 到登录页` → 见 C 类第 1 条，**实际为 navigateTo 压栈，reLaunch 仅失败兜底**）

### src/adapters/character/CharacterImporter.ts
- 删除：0 处
- 修正：1 处（`可直接传给 lorebookManager.matchFull 的 extraEntries` → 指向真实链路 `WorldInfoEngine.scan 的 entries`）

### src/services/builtinAssets.ts / src/services/backupService.ts / src/services/promptInfoStore.ts
- 无需改动（职责说明准确；内置资源移除的说明与现状一致）

### src/pages/cardpool/utils/cardpool_stats.js
- 删除：0 处 / 修正：0 处

### 其余文件
`src/main.js`、`src/App.vue`（除 2 条纯翻译标签）、`src/pages/*`（brand / collect / rules / characters / persona / presets(index) / session / login / test）、`src/components/**`（除 MessageItem / ReasoningBlock 的 1 条历史对照）、`src/utils/notify.ts`、`src/utils/streamPacing.ts`、`src/utils/navbar.js`、`src/utils/customCss.ts`、`src/utils/storage.js`、`src/stores/*`（userStore / regexPresetStore / noteStore / personaStore / moduleStore 除外项)：均已通读，**无需改动**。

---

## 三、待人工确认清单（C 类）

以下 14 处**一字未改**，等待裁决。

### 1. 守卫注释的入口方式（18 个页面重复出现）—— 建议统一订正
- 文件：`src/pages/import/import.vue:111` 等 **16 处逐字重复**（characters/index、characters/edit、monitor、chat、import/regex-edit、collect、index、rules、session、session/new-conversation、presets/index、presets/edit、persona/index、settings，另 `cardpool.vue:220` 为变体）
- 原注释：`// 需要登录：未登录会被 reLaunch 到登录页（守卫实现在 App.vue 的 checkUserLogin）。`
- 疑问：实际守卫走 `uni.navigateTo` **压栈**进登录页（`App.vue:191`），`reLaunch` 只在 navigateTo 失败时兜底（`App.vue:196`），以及登录成功后 `login.vue` 用 `reLaunch` 重建目标页。原措辞会被读成「替换当前页」。
- 建议：统一改为「未登录会被送到登录页（navigateTo 压栈，失败时 reLaunch 兜底）」。**因涉及 18 个页面，等你点头后我再统一改。**

### 2. `getNPCDef` 不存在（同一句话出现在两处）
- 文件：`src/utils/character_card/characterCardManager.js:207`
- 原注释：`按键直查世界书条目（供战斗 getNPCDef / 场景条目 / 道具条目等使用）`
- 疑问：`getNPCDef` 在全 `src` 无定义、无调用；`getLorebookEntryByKey` 也无任何消费方。但 `PromptBuilder.ts:524` 自述「最小版，Phase 3 接入真实战斗/任务摘要」——战斗是**计划中**还是**已废弃**？
- 关联：`src/stores/characterCardStore.ts:149` 有同一句措辞，需一起改。

### 3. 「尚未实现」与已实现页面矛盾
- 文件：`src/components/common/TabBar.vue:82`
- 原注释：`// today / collect / account 尚未实现`
- 疑问：`src/pages/collect/collect.vue` 是完整的 492 行「传奇收藏」页且已注册路由、首页可达。注释若指「这三个 Tab 未接入跳转」则成立；若指功能状态则已过时。

### 4. 品牌金色值与实际绘制值不符
- 文件：`src/pages/brand/brand.vue:292`
- 原注释：`// 金色 oklch(85% 0.15 85)。fillStyle 在循环外设一次，`
- 疑问：紧邻代码是 `ctx.fillStyle = 'rgb(234, 207, 140)'`，换算约 `oklch(86.2% 0.090 88.5)`；`oklch(85% 0.15 85)` 实际对应 `rgb(250,197,71)`，明显不是同一色。是「设计意图色名」还是「旧值残留」？

### 5. brand-spec 的 §3.5 锚点不存在
- 文件：`src/pages/brand/brand.vue:313`、`:658`
- 原注释：`// 系统开了「减弱动态效果」时只画一帧静态粒子，不做循环位移（brand-spec §3.5）。`
- 疑问：`notes/brand-spec.md` 中 §3 是编号列表，**没有 §3.5 小节**，但第 5 条内容与注释完全一致。应改为「§3 第 5 条」还是保留简写？

### 6. ContextInspector 的 chatId 无调用方传入
- 文件：`src/components/render/ContextInspector.vue:123`
- 原注释：`只看某个对话的快照（聊天页传入当前角色卡 id）。`
- 疑问：该组件仅被 `settings.vue:255` 挂载且**未传 `chatId`**；`props.chatId` 分支是活代码。是「曾经从聊天页打开」的残留，还是未来接入的契约文档？

### 7. DOMPurify 白名单描述不完整
- 文件：`src/utils/security.ts:10`
- 原注释：`仅允许基础排版标签，禁止 script/iframe/object/embed 及 on* 事件属性`
- 疑问：`FORBID_TAGS` 还禁了 `form/input/button`；`FORBID_ATTR` 只列了 4 个具体 `on*` 属性（其余由 `ALLOWED_ATTR` 白名单兜底）。是有意简化还是遗漏？

### 8. Persona 字段清单漏了 trpgProfile
- 文件：`src/utils/account/personaManager.js:6`（并关联同文件第 10 行存储结构清单）
- 疑问：注释与结构清单都只列 `{ id, name, avatar, description, createdAt, updatedAt }`，但 `create()` 会写入 `trpgProfile`、`update()` 还会深合并它，`personaStore.ts` 类型也带该字段。是「只指主要职责」还是漏写？

### 9. SSE 开关措辞
- 文件：`src/utils/llm/client.js:980`
- 原注释：`等实现真正的 SSE 解析后再打开这个开关。`
- 疑问：`_streamWithFetch` / `_consumeSSE` 已实现真正的 SSE 解析。但该注释所在的 `uni.request` 路径确实**永远无法**解析 SSE 分块。应改为「本条路径不支持 SSE，故不转发 stream」吗？

### 10. `// Sprint 1.5` 类迭代标记
- 文件：`src/stores/pluginStore.ts:3`（另 `presetStore.ts:3` 的 Sprint 1.7 已删、`settings.vue:673` 的 `Sprint 6.8`）
- 疑问：这 3 处是开发过程产物（可删），还是团队的迭代可追溯约定（应留）？

### 11. `RegexScriptEngine` 输入校验的用途描述
- 文件：`src/engine/RegexScriptEngine.ts:239`
- 原注释：`校验脚本的 findRegex 是否为合法正则（用于预设编辑页做输入校验）`
- 疑问：**此条我已按「与现状不符」修正**（全仓库无调用点，预设编辑页是只读展示）。保留在此供你复核是否同意改写。

### 12. 骰子助手的输出格式说明
- 文件：`src/utils/dice-helper.js:193`
- 原注释：`// ① 骰子表达式部分（如 "1d20+5" 或 "d6"）`
- 疑问：`count === 1` 时实际输出 `1d6` 而非 `d6`。是笔误还是按输入表达式举例？

### 13. 优劣式与修正值「不能同时存在」的措辞
- 文件：`src/utils/dice-helper.js:56`
- 原注释：`// 注意：优势/劣势与修正值不能同时存在（语义冲突），`
- 疑问：三组正则确实互斥，设计说明正确；但「语义冲突」与实际失配行为是否完全对应？

### 14. collect 物品行尾括注
- 文件：`src/utils/collect/collect-legend_items.js:38-39`
- 原注释：`usable: true, // 可通过背包交互（装备）`、`equipable: true, // 可装备（装备槽位类型）`
- 疑问：第 38 行的「（装备）」与 `InventoryPanel.vue` 中 `usable && !equipable` 才渲染「使用」按钮的行为有轻微出入；第 39 行括注描述的是下一行 `equipSlot` 的语义。是有意的笼统说明还是半句话残留？

### 被驳回的伪 C 类（19 处，未上报给你）
复核后确认**注释准确、无需改动**，包括：`item-types.js` 的稀有度图例重复、`common-items.js` 的 9 条分区/字段说明、`collect-trait.js` 的 `activeEffects` 预留字段、`CharacterExporter.ts` 的路径头、`item-manager.js`/`common-items.js` 中「通用物品所有故事可用」、`userScope.js` 的调用方核实等。为避免噪音未列入上表。

另有 2 条子 agent 上报的「存疑项」经我核实为**不存在的臆造问题**（例如声称 `chat.vue` 中有 `soundEffects` 相关注释 —— 该标识符在整个 `src` 中不存在），已直接驳回，未计入。

---

## 四、保留原则说明

按你的要求，以下类别**无论看起来多旧都默认保留**，本次清理严格遵循：

1. **「为什么这么写」的解释** —— 例如：
   - `chat.vue` 的流式帧率节流（39fps 对齐酒馆 Stopwatch）、平滑输出限速、显示态管线逐帧执行的原因、为何移除命令式滚动（会锁死用户滚动）
   - `client.js` 的 SSE 分块兜底、`resp.json()` 抛 SyntaxError 的踩坑记录
   - `App.vue` 的断网/503 不登出、`settings.vue` 的 token 预算
   - `defaultPreset.ts` 的 token 预算换算（4096 − 2000 回复预留 − 余量）
2. **跨模块 / 跨端 / 前后端约定** —— 例如：
   - `script.ts` 的酒馆原始 placement 编号对照 + `ST_TO_INTERNAL_PLACEMENT` 转换警示
   - `PromptBuilder.ts` 的各段 token 归属、`cachedStore.ts` 的迁移与 hydrate 契约
   - 所有 `#ifdef H5` / `#ifndef` 条件编译说明
   - `manifest.json` 的 uniStatistics 关闭理由（该文件不在清理范围，仅举例）
3. **安全 / 性能 / 隐私警示** —— 例如：
   - `userManager.js` 的「服务端权威、客户端传什么都不作数」、老账号认领不得采信 `isAdmin/isTest`
   - `security.ts` 的 DOMPurify 白名单、`BlockParser.ts` 的正则灾难性回溯限流
   - `cachedStore.ts` 的「最脆弱的一份数据」、防抖丢写兜底
4. **外部依赖的特殊处理** —— 例如：
   - `ai-helper.js` 的历史依据（解释现存活代码）、`uni.cloud.extend.AI` 无 endpoint
   - `cardpool_stats.js` 的 R2 `stats.json/comments.json` 数据源约定、R2 公开域名
   - 酒馆 `referencecode/...` 的对齐出处
5. **魔数 / 非直观逻辑解释** —— 例如：
   - `chat.vue` 的 `1000/30` 节流、`44px` 导航栏高度、`messagePageSize` 对齐酒馆 `chat_truncation`
   - `systemRegex.ts` 的 `{6,}` 正则坑、`vite.config.js`/`security.ts` 的阈值来历
6. **文件顶部职责与数据结构说明块**，以及**文件首行自身路径注释**（本仓库 101 个文件中 62 个使用的统一约定，本次一律未动）

**另外**：`迁移` 在本项目里大量是**活代码**（v1→v2 存档迁移、D20 跨账号认领、cachedStore hydrate 迁移、老本地账号认领）。这些说明全部保留；只删除了不含因果的「纯历史出处」子句。

---

## 五、附加发现（非注释问题，未擅自改动）

清理过程中发现一处**真实代码缺陷**，按「不要修改任何代码逻辑」的要求**未改动**，仅在此记录：

- 文件：`src/pages/cardpool/cardpool.vue:261`
- 现象：调用了 `useCharacterCardStore().reconcilePoolImports(this.cards)`，但该文件 **import 段（141–147 行）没有引入 `useCharacterCardStore`**。全仓库其它 9 个调用点（`session.vue`、`characters/index.vue`、`settings.vue` 等）都显式 import；`vite.config.js` 只挂 `uni()`，无自动导入插件。
- 后果：运行时会抛 `ReferenceError`，被紧邻的 `try/catch` 捕获后只打一条 `console.warn`，**静默失效** —— 即「给本功能上线前从卡池导入的历史卡片补来源标记」这段幂等兜底逻辑实际从未生效，用户仍可重复导入同一张卡。
- 该处**注释本身是准确的**（属「为什么这么写」），故保留；需要修的是代码（补一行 import）。

---

## 六、执行过程与质量保障

1. **事实底账先行**：在判断任何一条注释前，先核实用例 —— 用 `git log --all` 确认 `game.vue`、`character-manager.js`、`lorebookManager.js`、`ai-helper.js`、`stateManager`、`gameAI.js` 等**在本仓库全部历史中从未存在**（属并入前的旧工程引用，而非本仓库改名/删除），避免把「计划中」误判为「已废弃」。
2. **注释-only 门禁**：每处理一个文件，由独立的校验 agent 跑 `git diff --unified=0`，逐 hunk 确认被删行均为纯注释行、且代码 token 逐字符不变。
3. **最终全量验证**：对 101 个文件剥离全部注释后与 `HEAD` 比对代码 token → **96 个完全一致**，其余 5 个为本次任务前既有的未提交代码改动（已逐 hunk 核对无注释清理造成的代码丢失）。
4. **行尾统一**：`src` 下全部文件统一为 LF（仓库 `core.autocrlf=true`，该转换对 git 不可见，不产生额外 diff）。
5. **构建**：`npm run build:h5` → `DONE Build complete.`

> 注：`src/utils/collect/collect-legend_items.js` 的首行路径注释曾被误删，已恢复；`src/pages/chat/chat.vue` 中被过度删除的两处技术说明（`pages.json` 标题、续写按钮条件）亦已恢复。

---

## 七、清理过程中遇到的一个环境问题（供参考）

`npm run build:h5` 在受限沙箱下会报 `failed to load config from vite.config.js / spawn EPERM` —— 这是 Vite 的配置加载器需要派生子进程（esbuild）被沙箱拦截所致，**不是代码或配置错误**；在放开文件权限后构建正常通过。
